# Continue in Codex or Claude Code

Paste this prompt after opening the project folder:

> Continue building the Alongside autism-parent companion in this repository. I am not a programmer. Read AGENTS.md, docs/BUILD_STATE.md, docs/BUILD_BLUEPRINT.md and docs/SETUP.md first. Preserve completed source and tests; do not restart with another template. Use Vercel and a dedicated Supabase project. The fictional /preview walkthrough is available; live AI and database integration are not verified. The owner has supplied rateshsachdeva/bebigkid, giftingarena/bebigkid and Supabase flpzyiprlqloebxxiuyu. The schema is already installed. Resolve Vercel connector access to this project, configure secure environment variables and finish hosted integration using synthetic accounts. Do not create duplicate projects. Keep ALLOW_REAL_FAMILY_PILOT=false until every release gate has evidence and I approve the pilot. Never put secrets in chat, use my unrelated business projects, or describe sample replies as working AI. Work through the numbered release gaps, updating BUILD_STATE.md with actual test results. Supported model selection must stay in the owner dashboard, not in parent screens. Tell me plainly when a decision needs me; handle ordinary implementation yourself.

## Useful commands

`pnpm install --frozen-lockfile`, `pnpm typecheck`, `pnpm test`, `pnpm build`, `pnpm test:browser`.

`pnpm start` serves the production build. `/preview` makes no AI requests and holds fictional records in memory only. Never collect real family details during testing.
